// Bootstraps sbt-dependencies for this example build. To try unreleased changes, `sbtn publishLocal`
// in the repo root and set the SNAPSHOT version it prints here.
addSbtPlugin("com.alejandrohdezma" % "sbt-dependencies" % "0.30.0")
