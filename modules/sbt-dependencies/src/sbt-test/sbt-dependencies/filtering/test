# Verify updateDependencies filtering works
> compile

# Update only scalameta deps (munit)
> myproject/updateDependencies org.scalameta:

# Verify only munit was updated, typelevel deps unchanged
$ must-mirror project/dependencies.conf project/dependencies.conf.expected
