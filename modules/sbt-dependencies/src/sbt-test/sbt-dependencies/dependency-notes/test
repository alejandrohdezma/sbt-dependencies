# Verify dependency notes are preserved through updateDependencies
# Test case: notes (both single-line and multi-line) should survive
# version updates while maintaining their content.
# Uses dead http4s 0.21.x line (last release: 0.21.34) for determinism.

> compile

> myproject/updateDependencies

$ must-mirror project/dependencies.conf project/dependencies.conf.expected
