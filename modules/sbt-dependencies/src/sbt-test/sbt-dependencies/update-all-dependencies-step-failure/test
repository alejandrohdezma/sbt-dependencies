# Verify updateAllDependencies succeeds even when a step fails
> compile
> updateAllDependencies

# Verify that the step after the failure was skipped (marker file should NOT exist)
-$ exists step-after-failure-ran

# Verify that .sbt-update-report matches expected content
$ must-mirror expected-report target/sbt-dependencies/.sbt-update-report
