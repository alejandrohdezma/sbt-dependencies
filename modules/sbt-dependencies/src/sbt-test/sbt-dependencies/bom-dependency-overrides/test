# BOM pins are exposed through `dependenciesFromBom` but only reach `dependencyOverrides` when the build
# appends them, forcing transitive dependencies to the BOM's versions.
> assertNotOverridden
> assertOverridden
