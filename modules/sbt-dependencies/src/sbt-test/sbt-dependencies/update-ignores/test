# Verify dependencyUpdateIgnores excludes known-bad versions
# Test case 1 (default ignores): circe-yaml 1.15.0 is in Scala Steward's
#   default ignore list. Starting at 0.15.1, should update to 0.15.2.
# Test case 2 (local ignore file): http4s-core 0.21.34 is ignored via a local
#   HOCON file. Starting at ~0.21.32, should update to ~0.21.33.

> compile

> myproject/updateDependencies

$ must-mirror project/dependencies.conf project/dependencies.conf.expected
